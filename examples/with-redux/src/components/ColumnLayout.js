import qmlSource from './ColumnLayout.qml';
import { createQmlComponent } from '@react-qml/renderer';

const ColumnLayout = createQmlComponent(qmlSource, 'ColumnLayout');

export default ColumnLayout;
