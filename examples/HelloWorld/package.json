{
  "name": "HelloWorld",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "react-qml start --dev --platform=macos",
    "build": "NODE_ENV=production react-qml bundle --platform=macos"
  },
  "dependencies": {
    "react": "16.0.0",
    "react-qml-renderer": "^0.2.1"
  },
  "devDependencies": {
    "babel-preset-react-native": "^4.0.0",
    "react-qml-cli": "^0.2.2"
  }
}
